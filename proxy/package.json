{
  "name": "outfit-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that holds the Anthropic API key for the Outfit app.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260925.2",
    "typescript": "^5.9.0",
    "wrangler": "^4.140.0"
  }
}
